
GUI
AWT不同平台效果不一样，Swing一样。
新的窗体是一个线程！
窗体frame默认borderlayout，直接放按钮填充全屏。键盘的监听不认识shift例如，可以用getkeytext（e.getkeycode）。组合键可以用iscontroldown

OSI
物理层：网线，无线发射，传输介质，数电模电转换。这一层的数据叫比特。
数据链路层：网卡地址封装解封装，这一层是帧。网卡，（网线插的）交换机。交换机每个口也有mac地址。
网络层：IP地址封装，要发向哪一台主机，路由器，这一层叫数据包。
传输层：定义传输协议和端口号，将数据分包和传输再重组。这一层叫段。
会话层：建立回话。
表示层：解压 加密等，将数据转换为人能是别的。
应用层：应用软件，FTP等。
通讯要素：IP，端口号，协议
本地回环ip：127.0.0.1 localhost 访问本地。
端口：确定接受数据的进程，逻辑端口。物理端口指的是能连设备的。0-65535,0-1024win系统用
网关挂防火墙：凡是某端口的一律禁止通过，该进程失效。
java.net包。
服务器集群：一台机器ip地址不唯一。
若自己没有指定dns，那由网络商进行dns地址设置。
socket，套接字，插座，通讯的两端。不同的传输方式有不同socket，网络编程又称为socket编程。
udp：DatagramSocket。send方法参数datagrampacket类型，datagrampacket类的构造函数有地址，为发送端，无地址为接收端。
接收先启动，不然看不到效果，发送太快了
群发：把ip最后一段改成255。网络信息多样，所以是字节流。
文件或者读行的结束都要有结束标记，如果文件的缓冲区读满（8k默认），会被自动写入文件，而如果读不满且不刷新则不会写入，没有结束标记会永远等待。
tcp：socket类和serversocket类，建立回话后用socket对象的getoutputstream方法获取输出流，用此流的write方法可以输出，最后关闭socket，流对象也跟着关闭了

正则表达式容易理解，但是可读性差。若某一位一定是a 则只写a 。
正则中的\本身要被转义，例如\\d。.是任意字符，\也是表达式，所以要用\\. 。
组：组的组号https://blog.csdn.net/zhuche110/article/details/2233023   （.）任意字符的组，（.）\\1将本组称为1组（.）\\1+多个任意字符重复。
$符号，在函数参数中，第二个参数是$，则$取 前一个参数的第一组本次匹配的 字符串。
matcher的find方法不返回匹配字符串，匹配字符串可以用matcher.group取得。\\b[a-z]{3}\\b，找三个字母的单次，\\b是单词边界
regex包
